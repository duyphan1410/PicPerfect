@model PicPerfect.Models.Album

@{
    ViewData["Title"] = "Xóa Album";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h1>Xóa Album</h1>
    <hr />

    <div class="alert alert-danger">
        <h3>Bạn có chắc chắn muốn xóa album này?</h3>
        <p>Hành động này không thể hoàn tác. Tất cả ảnh trong album sẽ bị xóa khỏi album nhưng vẫn được giữ lại trong
            thư viện ảnh của bạn.</p>
    </div>

    <div class="card">
        <div class="card-header bg-danger text-white">
            <h3 class="mb-0">Thông tin Album</h3>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <dl class="row">
                        <dt class="col-sm-4">Tên Album</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.AlbumName)</dd>

                        <dt class="col-sm-4">Mô tả</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Description)</dd>

                        <dt class="col-sm-4">Ngày tạo</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.CreationDate)</dd>

                        <dt class="col-sm-4">Số ảnh</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.NumberOfImage)</dd>
                    </dl>
                </div>
                <div class="col-md-6">
                    <div class="text-center">
                        <h5>Ảnh bìa</h5>
                        <img src="@Model.CoverImage" alt="Cover Image" class="image-preview" style="max-width: 100%;" />
                    </div>
                </div>
            </div>

            <form asp-action="Delete" class="mt-4">
                <input type="hidden" asp-for="AlbumId" />
                <button type="submit" class="btn btn-danger">Xóa Album</button>
                <a asp-action="Details" asp-route-id="@Model.AlbumId" class="btn btn-secondary">Quay lại</a>
            </form>
        </div>
    </div>
</div>
