@model IEnumerable<PicPerfect.Models.Album>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<body>
    <section class="jumbotron text-center">
        <div class="container">
            <h1 class="jumbotron-heading">Album của tôi</h1>
            <p class="lead text-muted">Quản lý và xem các album ảnh của bạn</p>

            <p>
                <a asp-action="Create" class="btn btn-primary my-2">Tạo album mới</a>
            </p>
        </div>
    </section>
    <div class="album py-10 bg-light">
        <div class="container">
            <div class="row">
                @foreach (var album in Model)
                {
                    <div class="col-md-4">
                        <div class="card mb-4 box-shadow">
                            <img class="card-img-top" src="@album.CoverImage"
                                style="height: 225px; width: 100%; display: block;" data-holder-rendered="true">
                            <div class="card-body">
                                <h5 class="card-title">@album.AlbumName</h5>
                                <p class="card-text">@album.Description</p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <div class="btn-group">
                                        <a asp-action="Details" asp-route-id="@album.AlbumId"
                                            class="btn btn-sm btn-outline-secondary">Xem</a>
                                        <a asp-action="Edit" asp-route-id="@album.AlbumId"
                                            class="btn btn-sm btn-outline-secondary">Sửa</a>
                                        <a asp-action="Delete" asp-route-id="@album.AlbumId"
                                            class="btn btn-sm btn-outline-secondary">Xóa</a>
                                    </div>
                                    <small class="text-muted">@album.NumberOfImage ảnh</small>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</body>
